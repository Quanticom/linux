name: Date

on:
  workflow_dispatch: # on button click

jobs:
  date:

    runs-on: ubuntu-20.04

    steps:
      - name: Date
        env:
          personal_token: ${{ secrets.PERSONAL_TOKEN }}
        run: |
          date
          echo $PERSONAL_TOKEN
